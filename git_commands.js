const fun = add =(x,y) =>{
    return x+y
}

// git config --global user.name "username"
// git config --global user.email "useremail"
// git config --global --list -- to list the username and email configured




// git branch "branchname"-- for creating new branch
// git checkout "branchname" -- for switch to branch
// changes
// first commit changes
//git push(to particular branch)
